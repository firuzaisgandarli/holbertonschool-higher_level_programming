# Python - More Classes and Objects

This project is part of the **Holberton School Higher Level Programming** curriculum.  
It focuses on learning **Object-Oriented Programming (OOP)** concepts in Python, including:

- Private attributes
- Property getters and setters
- Class attributes
- Instance deletion
- String and repr representations
- Static and class methods

## 📂 Project Structure

- `0-rectangle.py` → Empty class `Rectangle`
- `1-rectangle.py` → Rectangle with width and height validation
- `2-rectangle.py` → Adds `area()` and `perimeter()` methods
- `3-rectangle.py` → String representation using `#`
- `4-rectangle.py` → `repr()` method for recreation with `eval()`
- `5-rectangle.py` → Detect instance deletion (`Bye rectangle...`)
- `6-rectangle.py` → Counts number of instances
- `7-rectangle.py` → Customizable `print_symbol`
- `8-rectangle.py` → Static method `bigger_or_equal()`
- `9-rectangle.py` → Class method `square()`

## 🛠 Requirements

- Python 3.8.5 (Ubuntu 20.04 LTS)
- All files executable (`chmod +x filename.py`)
- First line of each file: `#!/usr/bin/python3`
- Code style: **pycodestyle 2.7.\***
- Each file ends with a new line
- Documentation for modules, classes, and methods

## ▶️ Usage

Run any of the test files to check functionality:

```bash
./0-main.py
./5-main.py
./9-main.py
